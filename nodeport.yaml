apiVersion: v1
kind: Service
metadata:
  name: photogallery
  labels:
    run: photogallery
spec:
  type: NodePort
  ports:
    - port: 3000
      nodePort: 30036
      name: http
  selector:
    run: photogallery